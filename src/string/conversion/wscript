def build(bld):
	sources = [
		"int_conversion.c"
	]
	lftstrcvn = bld.shlib(
		source		= sources,
		features	= "c cshlib",
		target		= "ftstrcvn",
		includes	= bld.env.INCLUDES_ROOT
	)
	lftstrcvn.post()
