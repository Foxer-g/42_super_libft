def build(bld):
	sources = [
		"environment.c",
		"math.c"
	]
	lftutl = bld.shlib(
		source		= sources,
		features	= "c cshlib",
		target		= "ftutl",
		includes	= bld.env.INCLUDES_ROOT,
		use			= "ftstr"
	)
	lftutl.post()
